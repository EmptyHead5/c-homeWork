(define (folder f z xs)
  (if(null? xs)
     z
     (f (car xs) (folder f z (cdr xs)))))
(folder + 0 ' (1 2 3 4 ))

; recursion
; function can easyly use for mutiple,add min, diveries, but need to attion with start number z,when user use function to calculate 
; mutiple and diveries
